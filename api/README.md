# jobs-api

FastAPI service for `jobs.forkstech.com`. See the root repository README and
`docs/jobs-forkstech-spec.html` for the product and infrastructure context.

This package only contains the runnable service foundation at this stage.